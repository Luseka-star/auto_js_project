name: Test for Unit 11

run-name: Running tests for Unit 11

on:
  push:
    branches:
      - main
      - Unit-11
  pull_request:
    branches:
      - main

jobs:
  test:
    uses: ./.github/workflows/reusable.yml
    with:
      npm-command: 'test'